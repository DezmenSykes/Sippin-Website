"use client";

import { View } from "@react-three/drei";
import { Bounded } from "../Bounded";

const Carousel = () => {
  return (
    <Bounded className="carousel">
      <View></View>
    </Bounded>
  );
};

export default Carousel;
